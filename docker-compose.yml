services:
  frontend:
    container_name: andito_frontend
    build:
      context: .
      dockerfile: ./docker/frontend/Dockerfile
    environment:
      - API_URL=http://localhost:5050
    ports:
      - 8080:8080
    depends_on:
      - backend
    restart: unless-stopped
  backend:
    container_name: andito_backend
    build:
      context: .
      dockerfile: ./docker/backend/Dockerfile
    ports:
      - 5050:5000
    restart: unless-stopped
